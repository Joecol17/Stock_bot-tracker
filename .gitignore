# Secrets — never commit the real .env
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.venv/
venv/

# Bot output
trading_bot.log
trade_history.json
equity_history.json

# Analytics + discovery state (generated at run-time, not source)
*.db
universe.json
watchlist.json
core_watchlist.json
explore_watchlist.json
reports/

# Runtime state files (written at run-time, not part of source)
bot_state.json
bot_control.json
symbol_queue.json
symbol_prefs.json
tracked_positions.json
tunnel_url.txt
launcher.log
cloudflared.exe
ngrok.exe

# Named tunnel config (contains local credential paths — generated by setup_named_tunnel.bat)
tunnel_config.yml

# Claude Code internal workspace (worktrees, settings)
.claude/
